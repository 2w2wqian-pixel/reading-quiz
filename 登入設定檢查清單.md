# Google 帳號登入 ＋ 雲端設定一致性：檢查與實作說明

閱讀理解練習站 · 2026-09-18（程式已完成並通過測試，等待部署與 Firebase Console 設定）

---

## 一、目前狀態

### ✅ 已經確認正確（實測）
| 項目 | 狀態 | 實測依據 |
|---|---|---|
| Web API Key | ✅ | 39 字元、`AIzaSy` 開頭（讀檔量測，非目視） |
| 資料命名空間 | ✅ | `classCode = "default"` → `rq/default` |
| Realtime Database 規則 | ✅ | 未帶 token 讀 RTDB 根回 **HTTP 401 Permission denied** |
| 匿名登入 | ✅ | 已啟用，先前的 `CONFIGURATION_NOT_FOUND` 已解除 |
| `authDomain` | ✅ | 已寫入 `data/config.json`：`chinese-6e50a.firebaseapp.com` |

### ✅ 程式已完成（第一輪：Google 登入本體）
| 檔案 | 改了什麼 |
|---|---|
| `assets/js/core/backend.js` | 新增 `googleAuth`（Google 登入模組）、`findByGoogleUid`、`bindGoogle`、`unbindGoogle`、`setStudentClass`、`isAssigned`、`assignReason`、`classNames`；`loadConfig`／`publishConfig` 帶上 `authDomain`；名冊同步到 repo 前移除 email；診斷報告新增「Google 帳號登入設定」 |
| `assets/js/student.js` | 登入頁新增「使用 Google 登入」按鈕；新增綁定頁（兩種入口）；身分一律正規化為名冊 `stu_xxx`；指派判定改用共用規則 |
| `assets/js/teacher.js` | 名冊表新增「登入方式」欄（顯示 email／已綁定／帳號密碼）、「班別」與「解除綁定」按鈕；指派對話框可依班別分組並「全選本班」；設定頁新增 `authDomain` 欄位與 Google 開通步驟說明 |
| `assets/js/core/store.js` | 設定預設值新增 `fb.authDomain` |
| `assets/js/app.js` | 登出時一併登出 Google（共用裝置換人用） |
| `data/config.json` | 新增 `firebase.authDomain` |

### ✅ 程式已完成（第二輪：登入入口補齊 ＋ 設定一致性）
| 檔案 | 改了什麼 |
|---|---|
| `assets/js/student.js` | 註冊頁新增 Google 按鈕（`使用 Google 帳號登入／註冊`）＋「Google 電子郵件」欄位；抽共用 `googleBlock()`；`enterAsStudent()` 統一導回原頁；取消登入改中性提示 |
| `assets/js/app.js` | 導覽列新增 Google 登入入口（`appendGoogleEntry`，手機版唯一到得了的入口）；設定頁改走 `Settings.policy()`、新增 `assignOnly` 開關、按鈕改為「儲存並套用到所有裝置」、新增「設定一致性（本機 ↔ 已發佈）」表格 |
| `assets/js/core/store.js` | 新增 `POLICY_KEYS`（五個開關的唯一來源）、`Settings.policy()`／`setPolicy()`／`hasPolicy()`、`toBool()`；舊攤平欄位寫入時清除 |
| `assets/js/core/backend.js` | 帳號型同步鍵 `ACCT`／`ADDR`（同一人同一卷跨裝置落在同一格）；`loadConfig()` 政策改以已發佈值為準（本機表態過則保留）、不再自行算布林；新增 `configAudit()`；診斷新增兩項 |
| `tools/_test_google_bind.js` | 改載**真的** `store.js`；新增 ⑩–⑮（email 身分、單一來源政策、帳號型同步鍵、設定盤點）→ **67 項** |
| `tools/_test_sync_audit.js` | 假環境改成跟真的一樣（即時讀取的 Settings、真的 `IDBDatabase` 契約、in-memory `indexedDB`）→ **45 項** |
| `tools/_glogin.html` / `_run_glogin.js` | 新增導覽列入口、註冊頁入口、取消授權、導回原頁四段 → **51 項**；virtual-time 預算 20s → 60s |
| `.gitignore` | 移除 `tools/_g*.html`（會誤排除真正的測試頁 `_glogin.html`） |
| `data/config.json` | 補上 `classCodeIsNamespace: false` 與 notes 說明 |

### ⏳ 只有你能做的（Firebase Console）
| # | 位置 | 要做什麼 | 沒做的後果 |
|---|---|---|---|
| **A1** | Authentication → **Settings → 已授權網域** | 加入 `2w2wqian-pixel.github.io` | 學生按 Google 登入會直接被擋（`unauthorized-domain`）。**最關鍵** |
| **A2** | Authentication → Sign-in method → Google | 確認已啟用，並設定「**專案支援電子郵件**」 | 沒設通常無法完成啟用 |
| A3 | Realtime Database → 規則 | 維持 `auth != null`（目前已是這樣） | — |

`authDomain` 怎麼查：Firebase Console →「專案設定 → 一般 → 你的應用程式」，
長得像 `你的專案.firebaseapp.com`。程式裡已填好 `chinese-6e50a.firebaseapp.com`。

---

## 二、五個關鍵設計（已落實）

### 1. Google 登入不會告訴你「這是誰」→ 所以有「綁定」
Firebase 只回傳 UID、email、顯示名稱；名冊用的是 `stu_xxx`。兩者無法自動對應，
所以新增了一層綁定：學生第一次用 Google 登入時，會被要求把 Google 帳號綁到老師建立的帳號。

### 2. 身分 ID 永遠是名冊的 `stu_xxx`（不是 Google UID）
作答紀錄的鍵是 `sub::<quizId>::<studentId>`。若改用 UID 當身分，
**所有既有作答與批改會瞬間對不上**。所以 `session.id` 一律保持名冊 id —— 這點有測試守住。

### 3. Google 登入不是白名單
任何 Google 帳號都能通過 Firebase 驗證。真正的門檻是「老師先建名冊 + 一次性綁定」。
因此 `allowSelfRegister` 建議維持 `false`。

### 4. 「同一份作答」的鍵由**帳號**決定，不是裝置
雲端上的欄位名是 `ACCT.key('sub::<quizId>::<studentId>')`（FNV-1a 雜湊成安全字元），
而**不是**裝置自己產生的 `sub_x`。

為什麼要這樣：舊版在一台裝置上寫 `sub::q1::stu_1`、在另一台寫 `sub_x`，
於是同一個人同一份卷會變成**兩筆紀錄**；自動批改還可能把先前那筆的分數歸零。
改成由帳號推算之後，任何裝置算出來的欄位名都一樣，自然就是同一格。

`saveSubmission()` 另外會把本機那筆的舊 id 刪掉／改名，讓本機與雲端共用同一個鍵
（有一條測試專門守住「本機舊鍵已清除，不會留下重複紀錄」）。

### 5. 政策開關只有一個地方在定義
`store.js` 的 `POLICY_KEYS`。三處 UI（設定頁／試卷管理／名冊頁）都只是**讀寫它**，
不自己算 `x !== false`。理由與實際症狀見 §8.1。

---

## 三、實際使用流程

### 學生
1. 老師先在「④ 學生名冊」建立帳號（帳號／姓名／班別／密碼）。
2. 學生在登入頁按「**使用 Google 登入**」。
3. 系統查名冊有沒有綁過這個 Google 帳號：
   - **有** → 直接進站（之後都是一鍵登入）。
   - **沒有** → 進入綁定頁，兩種入口任選：
     - **① 用老師給的帳密**：輸入帳號密碼 → 驗證通過即完成綁定。（最推薦，身分最明確）
     - **② 從名單選自己**：輸入班級代碼 → 從名單點自己的名字。（適合低年級，老師在場為佳）

### 老師
- **識別**：名冊表的「**登入方式**」欄 —— 已綁定顯示 email，未綁定顯示「帳號密碼」。
  表頭下方有統計「Google N 人／帳號密碼 M 人」。
- **管理**：每位學生可「**班別**」／「重設密碼」／「**解除綁定**」／「刪除」。
  學生換 Google 帳號、或誤綁到別人時，用「解除綁定」即可（**作答紀錄不受影響**）。
- **分班**：用「班別」按鈕逐一設定（例如 `2A`）。填好班別之後：
- **指派作業**：試卷管理 → 指派 → 可勾「指派給全班」、或**整個班別一起勾**、或個別學生。
  整班都勾時會記進 `assignment.classes`，**該班之後新增的學生也會一併涵蓋**。
- **批改**：流程完全不變（依 `studentId` 對位）。

### 共用裝置提醒
Google 登入要「登出 Google」才能換人。建議：
**學生自己的手機／平板 → Google 登入；教室共用平板 → 用原本的帳號密碼登入。**
兩種方式並存，不必二選一 —— 帳號密碼登入**沒有被移除**。

---

## 四、隱私說明（重要）

`data/roster.json` 是 **GitHub Pages 上的公開檔案**（任何人都能讀）。
- 原本就只存放 `salt:hash`（不存明文密碼）。
- 綁定用的 `googleUid` 是隨機識別碼，本身不含個人資料，所以保留在公開檔（學生裝置靠它查綁定）。
- **學生的 email 不會寫進公開檔** —— email 只留在雲端與老師自己的裝置。
  （程式有測試守住這一點。）

另外「班級代碼」也一併發佈在公開設定裡，因為它是學生綁定時的門檻、
而且學生本來就會被告知。它**不是密碼**；真正的防線是老師可以隨時解除綁定。

---

## 五、部署方式

程式碼改動需要部署到 GitHub Pages 才會生效：

```
python tools/_api_publish.py <你的 GitHub Token> --force-data=data/config.json
```

> 改到 `data/`（老師的真資料目錄）時要加 `--force-data`，這是刻意的安全鎖 ——
> 它會先列出「遠端有、本機沒有的項目」供你核對，避免蓋掉你在瀏覽器裡剛發佈的試卷。
> 若遠端已往前推進（例如你剛在老師端按過「發佈」），再加上 `--force-base`。

---

## 六、驗證清單（部署後照順序做）

1. 老師端「⑤ 資料與同步 → ⑥ **雲端自我診斷**」→ 應看到
   「Google 帳號登入設定 ✅ authDomain chinese-6e50a.firebaseapp.com」。
2. 學生登入頁應出現「**使用 Google 登入**」按鈕（沒有 → authDomain 沒發佈成功）。
3. 用一個**未綁定**的 Google 帳號測試 → 應停在綁定頁。
4. 綁定完成後 → 名冊表該學生的「登入方式」應顯示 email。
5. 換另一台裝置／無痕視窗，用同一個 Google 帳號 → 應直接進站。
6. 老師端指派作業 → 勾一個班別 → 該班學生在自己的裝置上應看到作業。

---

## 七、設定總盤點：每一個值住在哪裡

**原則：同一個設定只能有一個「權威來源」。** 下表分成三類：

- **A 類｜只存在 Firebase Console**：程式讀不到、也不該有第二份。改動只能到 Console 改。
- **B 類｜只存在 repo 的 `data/config.json`**（公開）：學生裝置唯一一定讀得到的通道。
- **C 類｜只存在各裝置的 `localStorage`**：裝置自己的偏好。

| # | 設定 | 類 | 權威位置 | 路徑／鍵 | 誰會讀 |
|---|---|---|---|---|---|
| 1 | Firebase 專案 | A | Firebase Console | 專案 `chinese-6e50a` | — |
| 2 | Database URL | B | `data/config.json` | `firebase.dbUrl` ← 老師端 `fb.dbUrl` | 所有裝置 |
| 3 | Web API Key | B | `data/config.json` | `firebase.apiKey` ← 老師端 `fb.apiKey` | 所有裝置 |
| 4 | **資料命名空間** | B | `data/config.json` | `firebase.classCode`（空 → `default`） | 所有裝置讀寫 `rq/<此值>/` |
| 5 | Google 授權網域 | B | `data/config.json` | `firebase.authDomain` | 學生登入頁是否顯示 Google 按鈕 |
| 6 | 收集端網址 | B | `data/config.json` | `hook.postUrl` ← 老師端 `hook.postUrl` | 交作／註冊／草稿 |
| 7 | **自助註冊門檻** | B | `data/config.json` | `classCode`（最上層） | 學生註冊頁 |
| 8 | 五個政策開關 | B | `data/config.json` | `policy.*` ← `Settings.policy()` | 學生端（見下方 §8.1） |
| 9 | 授權網域白名單 | A | Console → Authentication → **Settings → 已授權網域** | 需含 `2w2wqian-pixel.github.io` | Google 登入是否被擋 |
| 10 | Google 供應商 | A | Console → Authentication → **Sign-in method** | 需為「已啟用」 | Google 登入能否發起 |
| 11 | 匿名供應商 | A | Console → Authentication → **Sign-in method** | 需為「已啟用」 | **所有**雲端讀寫（資料通道用它換 token） |
| 12 | RTDB 規則 | A | Console → Realtime Database → **規則** | `{"rules":{"rq":{".read":"auth != null",".write":"auth != null"}}}` | 跨裝置同步 |
| 13 | GitHub Token／repo | C | 老師裝置 `localStorage` | `rq.settings.v1` → `gh.*` | 只有老師端能發佈 |
| 14 | 老師密碼 | C | 老師裝置 `localStorage` | `rq.settings.v1` → `teacherPass` | 老師登入（**與雲端無關**） |
| 15 | 登入身分 session | C | 該裝置 `localStorage` | `rq.settings.v1` → `session` | 當前裝置 |
| 16 | 螢光筆／生詞本 | C | 該裝置 IndexedDB | — | 該裝置 |

### 7.1 兩個 classCode 是**兩件不同的事**（最常見的誤解）

| | `firebase.classCode` | 最上層 `classCode` |
|---|---|---|
| 用途 | **雲端資料的命名空間** | **自助註冊的門檻** |
| 讀寫路徑 | `rq/<此值>/**` | 不進路徑，只用來比對 |
| 不一致的後果 | 兩台裝置**各寫各的節點，永遠看不到對方** | 學生填不進註冊頁 |
| 要不要告訴學生 | 不用 | **要**（學生本來就會被告知） |

`data/config.json` 裡另有一個 `"classCodeIsNamespace": false` 的旗標，就是在標明
「最上層這個不是命名空間」。**不要把兩者填成同一個欄位。**

### 7.2 誰可以覆蓋誰

```
Firebase Console（A 類）       ← 只能手動改，程式無權
        ↓ 決定「能不能連上」
data/config.json（B 類）        ← 老師按「儲存並套用到所有裝置」發佈
        ↓ 「缺什麼補什麼」：本機已有值就不覆蓋（老師自己的設定優先）
該裝置 localStorage（C 類）     ← 學生／老師各自擁有
```

- **B 類裡的「連線設定」（#2–#6）**：本機沒填才會被公開設定補上。
  → 老師在自己電腦填好的值不會被 repo 蓋掉。
- **B 類裡的「政策開關」（#8）**：**以已發佈的值為準**（見 §8.1）。
  這是刻意的差別 —— 政策是「老師在老師端調的班級規則」，
  如果也照「本機優先」，老師改了政策學生端永遠不會生效。
- **老師裝置**（`gh.token` 有值）**整組不被公開政策覆蓋**，
  這樣老師在同一台電腦切到學生視角時，不會把自己剛調好的設定改掉。

---

## 八、本次盤點發現的不一致（已修正）

### 8.1 【嚴重】五個政策開關有三份定義 → 改成單一來源

| 項目 | 內容 |
|---|---|
| **檔案** | `store.js`（定義）、`app.js`（設定頁）、`teacher.js`（試卷管理頁＋名冊頁） |
| **問題** | `assignOnly`／`showAnswerAfterSubmit`／`allowRetake`／`enableHighlight`／`allowSelfRegister` 這五個開關，**每一個都在三個頁面各自用 `x !== false` 自己算一次預設值**。只要有人改了一處的預設（或漏改一處），三處就會互相矛盾。實際症狀：老師端顯示已開放、學生端仍被擋；或老師改了政策，學生端毫無反應（因為學生端的預設值把它蓋掉了）。 |
| **修正** | 在 `store.js` 建唯一一份 `POLICY_KEYS`（含預設值）。三處一律走 `Settings.policy(key)`／`Settings.setPolicy()`；`loadConfig()` 與 `publishConfig()` 也只從這一份讀寫。**舊的攤平欄位在寫入時會被刪掉**，不再有兩份並存的機會。 |
| **測試** | `_test_google_bind.js` ⑪、⑭（共 11 項） |

### 8.2 【嚴重】`Settings.get().policy` 把「預設」和「已儲存」混在一起

| 項目 | 內容 |
|---|---|
| **檔案** | `assets/js/core/store.js` → `Settings.get()` |
| **問題** | 原本寫 `policy: Object.assign({}, POLICY_KEYS, s.policy \|\| {})` —— 先併預設值。結果**沒被改過的開關也會長得像「使用者存過的偏好」**，`loadConfig()` 再也分不出「這台裝置已經表態」還是「只是套用預設」。實測症狀：公開政策永遠蓋不掉本機預設，學生端與老師端各看各的。 |
| **修正** | `policy` 只放**使用者真的存過的值**（缺的留 `undefined`）。要讀「有效值」一律走 `Settings.policy(key)`，由它補 `POLICY_KEYS`。新增 `Settings.hasPolicy(key)` 供 `loadConfig()` 判斷「這台是否表態過」。 |
| **測試** | `_test_google_bind.js` ⑭（6 項：`hasPolicy` 行為、儲存的 policy 不含未表態項） |

### 8.3 【中】布林轉換把 `0` 與 `'false'` 誤判

| 項目 | 內容 |
|---|---|
| **檔案** | `assets/js/core/store.js` → 舊版相容分支 |
| **問題** | 舊寫法 `s[k] !== false && s[k] !== 0 && s[k] !== 'false' ? !!s[k] : false` —— 邏輯繞且難驗，`'0'`、`undefined` 等邊界值行為不明確。 |
| **修正** | 抽成一個 `toBool(v, fallback)`：只認真正的布林、`0`/`1`、`"true"`/`"false"`，其餘一律回 fallback（＝`POLICY_KEYS` 的預設）。 |
| **測試** | 併入 ⑭ 的舊版相容項 |

### 8.4 【中】`configAudit()` 也在自己算第二次預設值

| 項目 | 內容 |
|---|---|
| **檔案** | `assets/js/core/backend.js` → `configAudit()` |
| **問題** | 盤點政策時寫 `String(pub.policy[k] !== false)` —— 又是一個 `!== false`。這正是 §8.1 要消滅的寫法，出現在「專門用來抓不一致」的函式裡。 |
| **修正** | 改成 `Settings.policy()` 取有效值、與 `pub.policy[k]` 直接比對；不再自行推導布林。 |
| **測試** | `_test_google_bind.js` ⑮（5 項：未讀到時不誤報紅字、值不同時要標不一致） |

### 8.5 【中】取消 Google 登入被當成「錯誤」

| 項目 | 內容 |
|---|---|
| **檔案** | `assets/js/student.js` → `afterGoogle()`、`Student.loginGoogle()` |
| **問題** | 使用者在 Google 視窗按取消 → 回傳 `null` → 顯示 **紅色** 錯誤 toast「沒有取得 Google 帳號資料，請再試一次」。但 `backend.js` 那側已經正確地把取消歸類為中性的 `rq/local-cancel`。**兩層講法不一致**：學生以為網站壞了，反而再點第二次。 |
| **修正** | 兩處都改為中性提示「已取消 Google 登入，尚未登入」（非紅色、3 秒自動消失）。真正的原因仍由 `GoogleAuth.lastError` 記著，下次開登入頁會說明一次。 |
| **測試** | `_glogin.html` ⑨（5 項） |

### 8.6 【中】`.gitignore` 把真正的測試頁一起排除掉了

| 項目 | 內容 |
|---|---|
| **檔案** | `.gitignore` 第 7 行 |
| **問題** | 規則寫 `tools/_g*.html`，本意是排除測試**產物**，但 `tools/_glogin.html` 是**真正的測試頁**（`_run_glogin.js` 跑的那一份）。實測 `git check-ignore -v tools/_glogin.html` → 命中 `.gitignore:7`。結果：全新 clone 之後瀏覽器測試直接跑不起來（找不到檔案），而且沒人會發現，因為本機一直都有那個檔案。 |
| **修正** | 移除 `tools/_g*.html`，只保留明確的產物樣式。`git check-ignore` 現在回 exit 1（不再被忽略）。**教訓：`.gitignore` 的萬用字元要對著「產物命名」寫，不要對著「目錄前綴」寫。** |
| **驗證** | `git check-ignore -v tools/_glogin.html` 不再命中；`git status` 顯示為未追蹤（可 commit） |

### 8.7 【小】`data/config.json` 缺 `classCodeIsNamespace`

| 項目 | 內容 |
|---|---|
| **檔案** | `data/config.json` |
| **問題** | `publishConfig()` 會寫出 `classCodeIsNamespace: false`，但**磁碟上這份檔沒有這個欄位**（是舊版發佈留下來的）。這個旗標正是用來標明「上層 classCode 不是命名空間」——少了它，下一個看到這個檔的人很容易把兩個 classCode 混用。 |
| **修正** | 補上 `"classCodeIsNamespace": false`，並在 `notes` 加一行說明兩者的差別。已 `JSON.parse` 驗證格式正確。 |
| **驗證** | `node -e "require('./data/config.json')"` 通過；欄位與 `publishConfig()` 的輸出形狀一致 |

### 8.8 【測試】測試環境本身說謊，掩蓋了 §8.2

| 項目 | 內容 |
|---|---|
| **檔案** | `tools/_test_sync_audit.js` |
| **問題** | 三個「假環境」寫得跟真的不一樣，導致測不出真問題（甚至會把對的產品測成紅的）：<br>① `STATE` 是 `resetEnv()` 當下的**快照**，`loadConfig()` 之後寫進 localStorage 的新值斷言看不到。<br>② `Store.ready()` 回傳 `true`，但真的會 resolve 成 `IDBDatabase` 物件 → 診斷報告誤判「此瀏覽器不支援 IndexedDB」。<br>③ 沒有 `window.indexedDB` → 真的 `Store` 降級成記憶體模式。<br>④ 有一條斷言寫死 `sub.id === 'sub1'`，但提交後 id 已被刻意改成帳號型同步鍵 `sub::q1::s1`。<br>⑤ 政策情境自相矛盾：先在該裝置設 `assignOnly: true`（＝已表態），又期望公開政策的 `false` 覆蓋它。 |
| **修正** | `STATE` 改用 `Proxy` **即時讀取** `Settings.get()`；`ready()` 回傳帶 `objectStoreNames` 的物件；補一個最小可用的 in-memory `indexedDB` shim；斷言改測新的帳號型鍵（並加驗「本機舊鍵已清除」）；政策情境改成「未表態裝置」＋另加一條「已表態不被覆蓋」。 |
| **結果** | 這個修正同時讓 §8.2 的真問題浮現出來（修好假環境後才看到政策沒生效）。<br>**教訓：假環境要跟真的一樣 —— 不然測的是假象，不是產品。** |

### 8.9 【測試】瀏覽器測試的 virtual-time 預算不足

| 項目 | 內容 |
|---|---|
| **檔案** | `tools/_run_glogin.js` |
| **問題** | `--virtual-time-budget=20000`。測試段落從 6 段增加到 10 段之後（每段都有「輪詢到狀態出現」的 `until()`），20 秒會被**中途截斷** —— 表現出來的症狀是「完全沒有結果輸出」，看起來像程式卡住。 |
| **修正** | 提到 60000，並把外層逾時 40s → 120s。同時在檔內註明「預算要跟著輪詢總量長大」。 |


---

## 九、環境區分（正式 vs 測試）

盤點結果：**目前沒有汙染風險，不需要改動。**

| 項目 | 正式 | 測試 | 是否分離 |
|---|---|---|---|
| 資料目錄 | `data/` | `tools/_e2e_data/` | ✅ 且已 git-ignore |
| Firebase 專案 | `chinese-6e50a` | 測試頁用 `fake.firebaseio.com` 攔截 | ✅ 完全離線 |
| 名冊 | `data/roster.json` | `FAKE_ROSTER`（寫在測試頁裡） | ✅ |
| 老師真資料 | 只在老師裝置 | 測試頁所有 `fetch` 都被攔掉 | ✅ 不可能寫入 |

測試頁（`tools/_glogin.html`、`tools/diagnose.html`）有一個刻意的設計：
**把所有 `fetch` 都在最前面攔掉**，所以「跑測試」不可能碰到老師的真資料。
新增測試時請維持這個做法。

⚠️ 唯一要注意的：`Published.base = 'data/'` 是**相對路徑**，
所以測試頁要放在 **repo 根目錄**才會解析正確（放 `tools/` 底下會指到錯的位置）。

---

## 十、部署後仍需要你手動確認的項目

### 必須做（不做則功能不會動）

| # | 在哪裡 | 做什麼 | 沒做的症狀 |
|---|---|---|---|
| **M1** | Firebase Console → Authentication → **Settings → 已授權網域** | 加入 `2w2wqian-pixel.github.io` | 學生按 Google 登入直接被擋（`auth/unauthorized-domain`） |
| **M2** | 部署程式到 GitHub Pages | `python tools/_api_publish.py <token> --force-data=data/config.json` | 網站還是舊版，看不到新的登入入口 |
| **M3** | 部署後：老師端「設定」頁 | 確認新的「**設定一致性（本機 ↔ 已發佈）**」表格全綠 | 有紅字 → 按「重新發佈設定給所有裝置」 |

### 建議確認（影響體驗，不影響能否運作）

| # | 確認什麼 | 怎麼確認 |
|---|---|---|
| M4 | 手機版導覽列的 Google 按鈕看得到、按得到 | 用手機開 `2w2wqian-pixel.github.io`，首頁右上角 |
| M5 | 在**非學生頁**（例如首頁或設定頁）按 Google 登入 → 登入後有回到原頁 | 手動走一次，看網址是否回到 `#/settings` 之類 |
| M6 | 同一個 Google 帳號在**兩台裝置**登入 → 看到同一份作答 | 一台交卷、另一台重新整理 |
| M7 | 在裝置 A 改一個政策開關 → 裝置 B 重新整理後跟著變 | 例如關掉 `allowRetake`，看學生端是否也不能重考 |
| M8 | 學生名冊的「Google 電子郵件」欄是否都填了 | 老師端「④ 學生名冊」。沒填的學生第一次按 Google 登入要多做一次綁定（不會壞，只是多一步） |
| M9 | 教室共用平板是否改用帳號密碼登入 | Google 登入要「登出 Google」才能換人；共用裝置建議用帳密 |

### 已知限制（不是 bug，是設計）

| 項目 | 說明 |
|---|---|
| `allowSelfRegister` 預設 `false` | Google 登入**不是**白名單，任何 Google 帳號都能通過 Firebase 驗證。真正的門檻是「老師先建名冊」。除非你確定要開放，否則維持 `false`。 |
| 同一個 email 不能註冊兩個學生 | 程式會直接拒絕（「這個電子郵件已經登記給…」）。要換人請先在名冊頁清掉該學生的 email。 |
| `googleUid` 會留在公開的 `roster.json` | 它是隨機識別碼、不含個資。**email 不會**寫進公開檔（有測試守住）。 |
| 老師密碼與雲端無關 | 存在老師裝置的 `localStorage`。換一台電腦要重設。 |

